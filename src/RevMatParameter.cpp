#include "RevMatParameter.hpp"


virtual void RevMatParameter::applyParameter(TreeAln& traln, vector<nat> model, ParameterContent &content) const
{
  for(auto &m : model)
    {
      
    }  
} 


virtual ParameterContent& RevMatParameter::extractParameter(const TreeAln &traln, int model)  const
{
  
}   

